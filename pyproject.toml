[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest]
addopts = ["-q"]

[project]
name = "proxcloud"
version = "0.1.0"
dependencies = [
  "requests>=2.0",
  "python-dotenv>=0.21",
  "PyYAML>=6.0"
]
